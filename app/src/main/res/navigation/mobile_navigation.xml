<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/mobile_navigation"
  app:startDestination="@+id/nav_feed">

  <fragment
    android:id="@+id/nav_feed"
    android:label="@string/menu_feed"
    android:name="edu.cnm.deepdive.viral.controller.ui.feed.FeedFragment"
    tools:layout="@layout/fragment_feed"/>

  <fragment
    android:id="@+id/nav_friends_list"
    android:label="@string/menu_friends_list"
    android:name="edu.cnm.deepdive.viral.controller.ui.friendsList.FriendsListFragment"
    tools:layout="@layout/fragment_friends_list"/>

  <fragment
    android:id="@+id/nav_messages"
    android:label="@string/menu_messages"
    android:name="edu.cnm.deepdive.viral.controller.ui.messages.MessagesFragment"
    tools:layout="@layout/fragment_messages"/>

  <fragment
    android:id="@+id/nav_map"
    android:label="@string/menu_map"
    android:name="edu.cnm.deepdive.viral.controller.ui.map.MapFragment"
    tools:layout="@layout/fragment_map"/>

</navigation>